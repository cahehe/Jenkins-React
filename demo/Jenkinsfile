pipeline{    
    agent {
        docker {
            image 'node:latest'
            // args '-p 3000:3000'
        }
    }

    stages{
        
        stage("run"){
            steps{
                sh """
                    echo `Hello World`
                    """
            }
        }
    }
}